## Scope

This system should not be in a directory called "net".

Anyway, this shouldn't be taken seriously.
Building a real browser from scratch is millions if not dozens of millions LOC.
And it would take so long to load pages in pilot.

I think this is a reasonable amount of work here, though the renderer could be definitely improved.

## AI

LLMs have entirely generated two files in that directory: `css_lexer_parser_test.lua` and `html_lexer_parser_test.lua`.
No other source files have been contaminated.
